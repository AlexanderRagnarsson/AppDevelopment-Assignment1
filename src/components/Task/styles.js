import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TaskText: {
    marginTop: 10,
    marginBottom: 10,
    padding: 10,
    flex: 1,
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: 'rgba(0, 0, 0, 0.1)',
  },
});
